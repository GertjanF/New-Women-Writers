<h1>Users</h1>

<div class="form" method="get">

<%= flash_notice %>

<form name="formnaam" method="get">

<%= navigation @users, "name" %>

</form>
</div>

<%= will_paginate @users %>

<% if editor? %>
	<!-- Add user button -->
	<%= link_to image_tag("add.png", :border=>0),  new_user_path, :title => "add new user" %>
<% end %>

	<%= no_records(@users) %>

	<div class="users_list" id="users_list">
		<%= render :partial => "userlist" %>
	</div>
	<div class="users_list" id="users_list">
		<%= will_paginate @users %>
	</div>
