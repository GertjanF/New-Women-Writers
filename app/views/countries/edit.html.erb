<h1>Edit country</h1>

<div class="form">
	<!-- # START:errors -->
	<%= error_messages_for :country -%>
	<!-- # END:errors -->

	<fieldset>
		<legend>Enter country details</legend>
		<% form_for :country, :url => { 
			:action => "update", 
			:id=>@country.id} do |form| %> 
			
			<%= render :partial => 'form', :object => form  %>
				<%= image_submit_tag ("accept.png", :alt => "save changes", :title => "save changes" , :class => 'image-submit') %>
				<%= link_to image_tag("cancel.png", :border=>0), {:action => 'show', id=>@country.id}, :title => "cancel changes"  %>
		<% end %>
	</fieldset>
</div>