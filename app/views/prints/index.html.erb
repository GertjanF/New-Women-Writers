<h1>Prints</h1>

<div class="form" method="get">

<%= flash_notice %>

<form name="formnaam" method="get">

<%= navigation @prints, "title of work" %>

</form>
</div>

<%= will_paginate @prints %>
		
<% if editor? %>
	<!-- Add print button -->
	<%= link_to image_tag("add.png", :border=>0),  new_print_path, :title => "add new print" %>
<% end %>

	<%= no_records(@prints) %>

	<div class="prints_list" id="prints_list">
		<%= render :partial => "printlist" %>
	</div>
	<div class="prints_list" id="prints_list">
		<%= will_paginate @prints %>
	</div>
